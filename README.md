# spreadsheet-calculator

```
1. This application is implemented using Java 1.8, can be imported as a Maven project
2. Compile the source -> can execute the script bin/setup
3. Run the application -> can execute the script bin/spreadsheet
4. Run the tests -> can execute the script bin/run_functional_tests

```
